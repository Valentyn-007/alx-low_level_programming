#include <stdio.h>
#include "main.h"

/**
 * print_sign - checks for alphabets
 *
 * @n: - parameter of the _abs function
 *
 * Return: 0 if successful
 */
int _abs(int n)
	{
	if (n < 0)
		{
		return (-n);
		}
	else
		{
		return (n);
		}
	}
