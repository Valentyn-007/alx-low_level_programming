#include <stdio.h>
#include "main.h"

/**
 * add - adds 2 integers
 *
 * @int : param for add fn
 *
 * Return: 0 if successful
 */
 int add(int a, int b)
 	{
	int result = a + b;

	return (result);
	}
